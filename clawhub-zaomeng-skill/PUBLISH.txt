ClawHub Publish Notes

Suggested metadata:
- Type: OpenClaw Skill
- Name: zaomeng-skill
- Display Name: 造梦技能
- Version: 3.0.0
- Category: Writing / Roleplay / Character Simulation

Risk posture:
- Self-contained skill package with embedded minimal runtime
- No runtime clone or external Git bootstrap required
- Still depends on local Python execution plus Python package trust (`PyYAML`, optional `tiktoken`, optional `ebooklib`)
- Explicit safety policy included

Pre-publish checks:
1. SKILL.md frontmatter is valid.
2. Embedded runtime entrypoint starts successfully.
3. Output schema and safety files are present.
4. Examples are coherent with schema.
5. No credentials or secrets in bundle.

Release note:
- 3.0.0: Embedded a minimal local zaomeng runtime directly inside the skill package, removed runtime Git bootstrap from the primary execution path, and switched skill guidance to the packaged `runtime/zaomeng_cli.py` entrypoint.
- 2.1.1: Fixed the auto-bootstrap guidance to pin the external zaomeng repository to commit `649f7466738f99d60c454e167835462215cffc7d`, reducing supply-chain drift during runtime bootstrap.
- 2.1.0: Switched skill guidance to the A+ bootstrap flow so agents first reuse a local zaomeng repo when present and otherwise clone the repository before executing the real CLI workflow.
- 2.0.0: Switched the persona workflow to markdown-first storage, introduced navigation/persona bundles plus runtime memory writes, and added natural-language chat intent routing for distill-before-chat and act/observe session setup.
- 1.0.9: Rewrote skill docs to state plainly that zaomeng is a local rule-based engine and that agents must use `chat --message`.
- 1.0.8: Hardened agent chat rules to require `--message` before any PTY or stdin fallback.
- 1.0.7: Added one-shot `chat --message` guidance for direct agent execution and aligned ClawHub chat rules.
- 1.0.6: Clarified interactive chat and confirmation-gated execution rules for ClawHub skill usage.
- 1.0.5: Aligned ClawHub package schema and examples with the current novel-scoped local workflow.
